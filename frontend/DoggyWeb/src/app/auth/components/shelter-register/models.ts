export interface ShelterRegisterData {
  website: string,
  name: string,
  address: string,
  description: string,
  accountNumber: string,
  payPalEmail: string,
}

export interface ShelterMainInfo {
  email: string,
  phoneNumber: string,
  website: string,
}

export interface ShelterDescription {
  name: string,
  address: string,
  description: string,
}

export interface ShelterLegalInfo {
  accountNumber: string,
  payPalEmail: string,
}